import {resolve} from "path";

export const TEMPLATES_DIR = resolve(__dirname, '../../templates')
export const FILES_DIR = resolve(__dirname, '../../files')
export const VALIDATION_DIR = 'validation'
export const RESOLUTION_DIR = 'resolution'
export const PRIVATE_DIR = 'private'
export const PUBLIC_DIR = 'public'

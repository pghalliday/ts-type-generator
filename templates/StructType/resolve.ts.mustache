export type {{{name}}} = undefined | {
{{#properties}}
    {{{name}}}: {{{type}}},
{{/properties}}
};

export function {{{initializer}}}(validated: {{{internalPrefix}}}Validated.{{{name}}}): {{{name}}} {
    return {
{{#properties}}
        {{{name}}}: {{{initializer}}}(validated.{{{name}}}),
{{/properties}}
    };
}

export function {{{resolver}}}(dest: {{{name}}}, src: {{{internalPrefix}}}Validated.{{{name}}}, resolvedReferences: {{{internalPrefix}}}References.ResolvedReferences): {{{name}}} | {{{internalPrefix}}}Util.ResolutionError {
    {{{internalPrefix}}}assert.ok(typeof dest !== "undefined");
{{#properties}}
    const resolved_{{{name}}} = {{{resolver}}}(dest.{{{name}}}, src.{{{name}}}, resolvedReferences);
    if (resolved_{{{name}}} instanceof {{{internalPrefix}}}Util.ResolutionError) return new {{{internalPrefix}}}Util.ResolutionError(`Error encountered resolving property: [{{{name}}}]`, resolved_{{{name}}});
    dest.{{{name}}} = resolved_{{{name}}};
{{/properties}}
    return dest;
}

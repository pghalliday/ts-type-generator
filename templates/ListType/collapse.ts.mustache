export type {{{name}}} = {{{type}}}[];

export function {{{collapser}}}(resolved: {{{internalPrefix}}}Resolved.{{{name}}}): {{{name}}} {
    const value: {{{name}}} = [];
    for (const index in resolved!) {
        value[index] = {{{typeCollapser}}}(resolved[index]);
    }
    return value;
}

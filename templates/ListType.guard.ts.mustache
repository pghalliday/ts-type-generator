function is{{{name}}}(value: unknown): value is {{{name}}} {
    if (!Array.isArray(value)) return false;
    for (const item of value) {
        if (!is{{{type}}}(item)) return false;
    }
    return true;
}
